---
# defaults file for vitalied.ntp

ntp_server: [
  #'ntp iburst',
]

ntp_restrict: [
  #'default ignore',
  #'-6 default ignore',
  '-4 default limited kod nomodify notrap nopeer noquery',
  #'-6 default limited kod nomodify notrap nopeer noquery',
  #'-4 default kod notrap nomodify nopeer noquery',
  #'-6 default kod notrap nomodify nopeer noquery',
  #'-4 default limited kod nomodify notrap nopeer',
  #'-6 default limited kod nomodify notrap nopeer',
  '127.0.0.1',
  #'::1',
  #'192.168.1.0 mask 255.255.255.0 notrust'
]

ntp_broadcast: [
  #'192.168.1.255'
]
